{
  "name": "chromium-cli-server",
  "version": "1.0.0",
  "description": "Bridge server for chromium-cli (HTTP <-> WebSocket)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
